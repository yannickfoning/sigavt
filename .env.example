# Copier ce fichier en .env et remplir les valeurs réelles
# Base de données
DB_HOST=localhost
DB_PORT=3306
DB_NAME=sigavt
DB_USER=root
DB_PASSWORD=

# MySQL root (seulement pour docker-compose)
MYSQL_ROOT_PASSWORD=root_secret_2025

# JWT — générer avec : openssl rand -base64 64 (ou équivalent PowerShell)
JWT_SECRET=hpQfezCGirbfSSNizTdFQ13elnQ8ZCbMVO9so5uNl9xbLaYzbsMg6Pfz4U6d530ViF7A5lZmRchoA3Mm

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:8080,http://localhost:8081

# Environnement
SPRING_PROFILES_ACTIVE=mysql
PORT=8081
